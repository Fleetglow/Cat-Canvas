# Cat Canvas 画布性能优化记录

> 状态：**本轮优化已完成，进入真实使用观察期**  
> 更新日期：2026-07-31  
> 现役分支：`main`

## 当前结论

本轮优化已解决大量图片、节点和连线场景下的主要卡顿。节点创建、删除、连线、框选、拖拽和平移已达到当前可接受水平。

暂不继续改渲染架构。先真实使用数天，记录可稳定复现的问题和性能数据，再决定是否进入下一轮，避免为未证实的瓶颈增加复杂度。

## 已完成

### 1. RAF 合帧

高频鼠标事件不再同步触发无限次连线和选区重绘，统一合并到浏览器动画帧。

覆盖：

- 节点拖拽
- 节点缩放
- 连线拖拽
- 滚轮与快捷键缩放
- 选区辅助几何

### 2. 视口裁剪

`render()` 只创建当前视口及缓冲区域内的节点 DOM，画布外节点不参与节点渲染。

配套处理：

- 平移结束后立即渲染新进入视口的节点
- 裁剪外节点使用数据估算端口坐标
- `refreshNodes()` 跳过未挂载的裁剪外节点
- 小地图继续读取完整节点数据

### 3. 端口位置缓存

缓存节点原点到输入、输出端口中心的世界坐标偏移。节点拖拽时通过 `node.x/y + offset` 计算连线端点，不再逐帧执行大量 `querySelector()` 和 `getBoundingClientRect()`。

缓存失效场景：

- 节点缩放
- 节点高度自动调整
- 图片、字体或内容加载引起布局变化
- 节点 DOM 重建

### 4. 连线 DOM 增量更新

每条连接复用已有 DOM：

- 可见 SVG path
- 透明命中 path
- 删除按钮

新增连接才创建 DOM；删除连接只移除对应 DOM；路径、按钮位置、选中态和悬浮态只在变化时更新。临时连线也复用同一个 path。

### 5. 节点创建增量插入

创建单个节点时不再全量重建全部节点 DOM，只插入新节点并刷新必要几何和图标。

### 6. 平移优化

- mousemove 经 RAF 限制到屏幕刷新节奏
- 平移过程中不重建节点
- 平移过程中不全量刷新小地图
- 临时提升 `.world` 合成提示，松手后恢复
- 松手后同步最终 viewport，并立即刷新节点和小地图

### 7. DOM 批量插入

节点全量渲染使用 `DocumentFragment`，减少逐个挂载造成的重复 DOM 开销。

## 已修复问题

- 节点多时创建、删除、连线和框选反应慢
- 拖拽节点时连线重绘卡顿
- 视口内图片多时操作明显掉帧
- 平移时偶发跳位
- 平移到新区域后只显示连线、不显示节点
- 平移结束后小地图视口矩形不更新
- 首次进入画布时小地图为空
- pan 结束流程因空事件访问中断，导致后续渲染未执行

## 本轮改动

- 视口裁剪、RAF 节流、端口缓存与平移刷新修复
- 连线 DOM 增量渲染

## 观察期测试清单

真实使用时重点记录：

1. 画布节点、图片和连线数量
2. 哪个操作出现卡顿：平移、缩放、拖拽、创建、删除、框选、撤销或切换画布
3. 是否稳定复现，还是偶发
4. 是否只在低缩放比例或大量图片同时可见时发生
5. 节点、连线或小地图是否出现错位、残留、消失
6. 浏览器 Performance 录制中主要耗时属于 Scripting、Layout、Paint 还是 GPU 合成

建议观察规模：

- 100–200 节点：日常使用
- 300–500 节点：压力使用
- 500+ 节点：超大画布

## 暂不实施

以下优化保留为候选，不在观察期内继续开发。

### 节点 DOM keyed 复用

当前 `render()` 仍会重建视口内节点。下一轮若平移结束、缩放结束、撤销或删除仍有明显停顿，可保留未变化节点 DOM，只增删变化节点，避免图片重新挂载和解码。

风险：节点类型多，Output、LLM、Generator、图片加载和自动高度变化都需要正确的局部更新策略。

### 节点移动改用 transform

当前节点拖拽仍更新 `left/top`。只有性能录制确认 Layout 是主要瓶颈时，才考虑改为 transform 临时位移、松手后写回坐标。

风险：端口坐标、缩放、框选、小地图和保存坐标需要保持一致。

### 小地图改用 Canvas 2D

当前小地图仍以 DOM 矩形表示全部节点。若 500+ 节点时小地图刷新成为明确瓶颈，可改用单个 `<canvas>` 绘制。

### Canvas 2D / WebGL 渲染层

仅在现有 DOM 架构无法满足真实规模时考虑。属于高成本架构重构，不提前实施。

## 下一轮启动条件

满足任一条件再继续优化：

- 真实使用中出现稳定可复现的卡顿
- Performance 录制能定位明确瓶颈
- 当前节点规模无法满足实际工作流
- 出现连线缓存、视口裁剪或小地图状态错误

在此之前：**保持现状，不继续增加缓存、抽象或渲染分支。**
