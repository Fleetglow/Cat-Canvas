# Cat Canvas 画布性能优化方案

## 问题诊断

当前画布在节点很多时出现卡顿，核心问题：

1. **连线全量重绘**：每次拖拽 mousemove 调用 `renderLinks()`，清空并重建所有 SVG 连线
2. **节点全量重绘**：`render()` 时 `innerHTML = ''` 清空所有节点再重建
3. **无视口裁剪**：画布外的节点和连线也参与渲染计算
4. **频繁 DOM 查询**：`portPoint()` 每条连线调用 2 次 `querySelector` + `getBoundingClientRect`
5. **几何计算未缓存**：`refreshGeometry()` 被多次调用，每次重算所有连线路径

## 优化方案（按优先级）

### 🔥 优先级 1：增量更新连线（影响最大）

**当前代码**（static/js/canvas.js:8016-8029）：
```javascript
function renderLinks(){
    linksEl.innerHTML = '';  // ❌ 清空所有
    linkControlsEl.innerHTML = '';
    connections.forEach(c => {
        const a = portPoint(c.from, 'out'), b = portPoint(c.to, 'in');
        linksEl.appendChild(pathEl(a.x, a.y, b.x, b.y, 'link'));
        // ...
    });
}
```

**优化方案**：
- 为每条连线创建时设置 `dataset.connectionId`，拖拽时只更新相关连线的 `d` 属性
- 缓存连线 DOM 元素到 Map，避免重复创建/删除

**预期效果**：拖拽 100 个节点时，从重建 500 条连线降低到只更新约 20 条相关连线

---

### 🔥 优先级 2：节点位置更新用 transform 代替 left/top

**当前代码**（static/js/canvas.js:7584-7588）：
```javascript
dragNode.node.x = dragNode.ox + dx;
dragNode.node.y = dragNode.oy + dy;
el.style.left = `${dragNode.node.x}px`;  // ❌ 触发重排
el.style.top = `${dragNode.node.y}px`;
```

**优化方案**：
- 拖拽时只修改 `transform: translate()`，不改 left/top
- 松开鼠标时才写回 x/y 坐标
- 给节点容器添加 `will-change: transform`

**预期效果**：拖拽流畅度提升 60-80%

---

### 🚀 优先级 3：视口裁剪（Viewport Culling）

**实现逻辑**：
1. 计算当前视口可见区域（已有 `currentWorldViewRect()`）
2. 渲染节点时，画布外的节点设置 `display: none` 或 `visibility: hidden`
3. 连线只渲染两端至少有一个在视口内的

**代码位置**：
- 修改 `render()` 和 `refreshNodes()` 添加视口判断
- 修改 `renderLinks()` 过滤不可见连线

**预期效果**：
- 1000 节点场景，可见区域只有 50 个节点时，渲染开销降低 95%
- 需注意 minimap 仍需完整数据

---

### ⚡ 优先级 4：缓存端口坐标

**当前代码**（static/js/canvas.js:8004-8015）：
```javascript
function portPoint(id, kind){
    const n = nodes.find(x => x.id === id);  // ❌ 每次遍历
    const el = nodesEl.querySelector(`.node[data-id="${id}"]`);  // ❌ DOM 查询
    const port = el.querySelector(`.port.${kind}`);  // ❌ DOM 查询
    const r = port.getBoundingClientRect();  // ❌ 强制同步布局
    // ...
}
```

**优化方案**：
- 创建 `portCache = new Map()` 缓存每个节点的端口坐标
- 节点移动/缩放时只更新受影响节点的缓存
- 连线渲染时直接从缓存读取

**预期效果**：连线重绘时间从 O(N²) 降至 O(N)

---

### 🎯 优先级 5：DocumentFragment 批量插入

**当前代码**（多处）：
```javascript
connections.forEach(c => {
    linksEl.appendChild(pathEl(...));  // ❌ 逐个插入
});
```

**优化方案**：
```javascript
const frag = document.createDocumentFragment();
connections.forEach(c => frag.appendChild(pathEl(...)));
linksEl.appendChild(frag);  // ✅ 一次插入
```

**预期效果**：DOM 批量操作提速 30-50%

---

### 🔧 优先级 6：requestAnimationFrame 节流

**当前问题**：
- `onNodeDrag` 在 mousemove 中直接调用 `renderLinks()`
- 高频鼠标事件触发过多无效渲染

**优化方案**：
```javascript
let rafPending = false;
function onNodeDrag(e){
    // 更新数据
    dragNode.node.x = dragNode.ox + dx;
    // 节流渲染
    if(!rafPending){
        rafPending = true;
        requestAnimationFrame(() => {
            rafPending = false;
            renderLinks();
        });
    }
}
```

**预期效果**：渲染频率从 200fps 降至 60fps，CPU 占用减半

---

### 🌟 优先级 7：虚拟化节点列表（长期方案）

适用场景：超过 500 个节点的超大画布

**技术选型**：
- 使用 Canvas 2D API 替代 DOM 节点（类似 Figma/Miro）
- 或使用 WebGL 渲染（类似 tldraw）
- 保留 DOM 交互层处理事件

**预期效果**：支持 10000+ 节点无卡顿

**实施成本**：需要重构整个渲染架构（3-4 周工作量）

---

## 快速实施方案（1-2 天）

**Phase 1（4小时）**：
1. 优先级 1：增量更新连线
2. 优先级 2：Transform 代替 left/top
3. 优先级 6：RAF 节流

预期提升：**节点 < 200 时基本流畅**

**Phase 2（1天）**：
4. 优先级 3：视口裁剪
5. 优先级 4：端口坐标缓存

预期提升：**节点 < 1000 时可用**

**Phase 3（按需）**：
6. 优先级 5：批量 DOM 操作
7. 优先级 7：Canvas 渲染重构（超大画布场景）

---

## 测试建议

创建性能测试用例：
- 100 节点 + 200 连线：期望 60fps 流畅拖拽
- 500 节点 + 1000 连线：期望 30fps 可用
- 1000 节点 + 2000 连线：期望 15fps 不卡死

测量指标：
- Chrome DevTools Performance 录制拖拽操作
- 关注 Rendering/Painting/Scripting 时间
- Layout Thrashing 警告

---

## 风险提示

1. **增量更新可能遗漏边界情况**：需要完整测试删除节点、撤销/重做等场景
2. **Transform 坐标系问题**：注意与 viewport.scale 的交互
3. **视口裁剪影响小地图**：需要独立的 minimap 渲染逻辑
4. **缓存失效策略**：节点添加/删除/连接变化时需清理缓存

---

## 参考实现

类似项目的性能优化案例：
- **Excalidraw**：使用 Canvas 渲染 + 虚拟化
- **tldraw**：WebGL 渲染 + 四叉树空间索引
- **React Flow**：虚拟滚动 + 连线路径缓存
- **Mermaid Live Editor**：SVG 渲染 + 增量更新

建议先实现 Phase 1 的三个优化，测试效果后再决定是否继续 Phase 2。
